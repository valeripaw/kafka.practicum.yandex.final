spring:
  docker:
    compose:
      enabled: false
  datasource:
    shop:
      url: jdbc:postgresql://localhost:5432/shop_db
    client:
      url: jdbc:postgresql://localhost:5432/client_db

kafka:
  # кластер №1
  bootstrap-servers: localhost:9092,localhost:9093
  schema-registry-url: http://localhost:8081

client-api:
  use-command-line: true
